# Changelog

Append-only log of notable changes to this vault. Each agent logs its own, with
its tag. Git already has the full history — this is the human-readable highlight reel.

- `YYYY-MM-DD` [AGENT-A] initialized brief, started draft
- `YYYY-MM-DD` [AGENT-B] first audit pass, opened C-01
- `YYYY-MM-DD` [BOTH] resolved C-01 via official docs, shipped final report
